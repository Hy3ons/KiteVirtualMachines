# CDI

CDI provides the `DataVolume` API used to import and clone VM disks.

```sh
build/deploy/scripts/install-cdi.sh
build/deploy/scripts/wait-cdi.sh
```

Override the installed version with:

```sh
CDI_VERSION=<version> build/deploy/scripts/install-cdi.sh
```
