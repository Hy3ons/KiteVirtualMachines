apiVersion: apps/v1
kind: Deployment
metadata:
  name: kite-controller
  namespace: kite
  labels:
    app: kite-controller
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kite-controller
  template:
    metadata:
      labels:
        app: kite-controller
    spec:
      serviceAccountName: kite-controller
      containers:
        - name: kite-controller
          image: ghcr.io/hy3ons/kite-controller:latest
          imagePullPolicy: IfNotPresent
