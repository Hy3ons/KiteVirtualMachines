apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: kite-longhorn-disk-directory
  namespace: longhorn-system
  labels:
    app.kubernetes.io/name: kite-longhorn-disk-directory
    kite.anacnu.com/managed-by: kite-storage
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: kite-longhorn-disk-directory
  template:
    metadata:
      labels:
        app.kubernetes.io/name: kite-longhorn-disk-directory
        kite.anacnu.com/managed-by: kite-storage
    spec:
      hostPID: true
      tolerations:
        - operator: Exists
      containers:
        - name: mkdir
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              mkdir -p /host/mnt/kite-longhorn
              chmod 700 /host/mnt/kite-longhorn
              sleep 5
          securityContext:
            privileged: true
            runAsUser: 0
          volumeMounts:
            - name: host-mnt
              mountPath: /host/mnt
      volumes:
        - name: host-mnt
          hostPath:
            path: /mnt
            type: DirectoryOrCreate
